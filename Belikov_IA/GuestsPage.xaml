<Page x:Class="Belikov_IA.GuestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Belikov_IA"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Гости">

    <Page.Resources>
        <!-- Стили в едином стиле проекта -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#E94560"/>
            <Setter Property="BorderBrush" Value="#E94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#2A2A2A"/>
            <Setter Property="AlternatingRowBackground" Value="#333333"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E94560"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>

       
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="ИНФОРМАЦИЯ О ГОСТЯХ"
                   Grid.Row="0"
                   FontSize="16"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"
                   Foreground="#E94560"/>

        <!-- DataGrid -->
        <DataGrid Name="GuestsDataGrid" 
                  AutoGenerateColumns="False"
                  Grid.Row="1"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding first_name}" Width="150"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding last_name}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="200"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding phone}" Width="150"/>
                <DataGridTextColumn Header="Номер паспорта" Binding="{Binding document_number}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Информационная панель -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <TextBlock Text="Всего гостей: " Foreground="White" FontFamily="Consolas"/>
            <TextBlock x:Name="GuestsCount" Text="0" Foreground="#E94560" FontFamily="Consolas" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Page>
