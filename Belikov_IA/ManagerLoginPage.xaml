<Window x:Class="Belikov_IA.ManagerLoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход для владельца" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0C0C17" Offset="0"/>
            <GradientStop Color="#1B1B2F" Offset="0.3"/>
            <GradientStop Color="#533483" Offset="0.7"/>
            <GradientStop Color="#E94560" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="#E94560"/>
            <Setter Property="BorderBrush" Value="#E94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#E94560" Offset="0"/>
                        <GradientStop Color="#CC0000" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#660000"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="#CC1A1A2E" Margin="34,14,15,10" Padding="25" CornerRadius="10"
                BorderBrush="#E94560" BorderThickness="2">
            <StackPanel>
                <TextBlock Text="ВХОД ДЛЯ ВЛАДЕЛЬЦА" 
                          FontSize="18" FontWeight="Black" 
                          HorizontalAlignment="Center" Margin="0,0,0,20"
                          Foreground="#E94560" FontFamily="Impact"/>

                <TextBlock Text="Пароль владельца:" Margin="0,0,0,5"/>
                <PasswordBox x:Name="ManagerPasswordBox" 
                            Margin="0,0,0,20"/>

                <Button Content="ПОДТВЕРДИТЬ" 
                        Click="ConfirmButton_Click"
                        Width="120"
                        IsDefault="True"/>

                <Button Content="ОТМЕНА" 
                        Click="CancelButton_Click"
                        Width="120" Margin="0,10,0,0"
                        Background="#333333" Height="21"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>